{#
Fields:
- block_src (file): { label: 'Vidéo', accept: '.mp4'}
- block_ref (text): { label: 'Légende', min: 3, max: 64, required: true }
#}
<figure class="video-block">
  <video class="video-block__video">
    <source src="{{ media.path }}" type="video/mp4">
    Votre navigateur n'arrive pas à lire le MP4, cependant vous pouvez toujours le
    <a href="{{ media.path }}">Télécharger</a>
  </video>
  <figcaption class="video-block__legend">{{ json.block_ref }}</figcaption>
</figure>
