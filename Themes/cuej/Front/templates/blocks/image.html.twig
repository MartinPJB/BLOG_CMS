{#
Fields:
- block_src (file): { label: 'Image', accept: '.png, .jpg, .jpeg, .gif, .webp, .svg', max: '20MO'}
- block_size (select): { label: 'Taille de l'image', options: 'Dans les marges, Pleine largeur, Moyen, Tout petit' }
- block_alt (text): { label: 'Texte alternatif', min: 3, max: 256, required: true }
- block_ref (text): { label: 'Légende', min: 3, max: 64, required: true }
#}
<figure class="image-block col gap-1 center-text">
  <img class="image-block--{{ json.block_size|first|lower == 'p' ? 'p center-x' }}" src="{{ media.path }}" alt="{{ json.block_alt|raw }}">
  <figcaption class="legend fs-i">
    {{ json.block_ref|clean_content_for_html|raw }}
  </figcaption>
</figure>
